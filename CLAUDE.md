# Outbound Productivity Bridge — 대시보드 저장소

이 저장소는 **배포되는 대시보드 파일만** 담고 있습니다. 엑셀 모델·구버전 파일 등 나머지 산출물은 사용자의 로컬 PC(Google Drive)에만 있습니다.

## 사용자에 대해

- 사용자(Ian)는 물류 대시보드 기획·데이터에는 능숙하지만 git/배포 같은 개발 인프라는 낯설어함.
- 기술 설명은 쉬운 말로, 절차는 클릭/명령 단위로 안내할 것. 안 되는 게 있으면 "고장이 아니라 원래 설계"임을 먼저 말할 것.
- 한국어로 대화.

## 프로젝트 개요

센터별 출고 생산성 브릿지 대시보드. 설계 기준 생산성(UPH)에서 물량·인력·운영 제약을 순차 차감해 최종 예상 생산성을 구하고, 실제 실적과 대조한다.

- 계산식: `최종 예상 = 기준 × Π(1 − 제약률ᵢ)`. 각 손실 UPH = 직전 단계 잔여 × 제약률. 적용 순서: Volume → Workforce → Operations.
- **실행 갭** = 실적 − 전체 제약 적용 시 예상. 화면의 제약 ON/OFF 토글과 무관하게 고정(전체 적용 기준). 양수 = 현장이 모델보다 잘함.
- **제약별 최우수 센터** = 그 제약의 제약률이 가장 낮은 센터 (벤치마킹 대상).
- 현재 데이터는 전부 샘플. 실데이터 교체 예정.

## 이 저장소의 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 대시보드 본체 (단일 파일, 이것만 고치면 됨) |
| `404.html` | index.html과 **완전히 동일한 사본** — index를 고치면 반드시 함께 복사할 것 |
| `.assetsignore` | `.md`·`.vscode`를 공개 배포에서 제외 (저장소는 private이지만 **사이트는 공개**이므로 필수) |
| `robots.txt` | 검색엔진 차단 |
| `_headers` | Cloudflare 헤더 설정 |
| `실행방법.md` | PC/랩탑에서 로컬 실행하는 법 (사용자용) |
| `.vscode/` | VS Code 실행 태스크·권장 확장 |

## 배포

Cloudflare Workers 정적 배포가 GitHub `main`에 연동 — **main에 push하면 약 45초 뒤 자동 재배포**. 빌드 없음.

- 라이브: https://performance-view.nglim-psa.workers.dev
- 저장소: https://github.com/Rocket-Ian-lab/Performance-view (**private**)
- 사이트 자체는 아직 공개 — Cloudflare Access(사내 이메일 제한) 미적용 상태. 실데이터 투입 전 필수.

## 대시보드 기능 (v3, 한/영 전환)

- 단일 HTML, 외부 라이브러리 없음(차트는 인라인 SVG, 폰트만 Google Fonts).
- 구성: 센터/제약 칩 → KPI 6종 → 브릿지 워터폴 → 센터 비교 + 히트맵 → 실행 갭 → 제약별 대응력 → 상세 표. 라이트/다크 자동.
- 제약 추가/삭제 UI: ＋ 제약 추가 폼, Manage 목록에서 삭제.
- 엑셀 업로드: 순수 JS zip 파서 + `DecompressionStream('deflate-raw')`. "ID" 헤더 행으로 센터 표(ID+기준/Base)와 제약 표(ID+적용/Active+센터ID 열) 인식, 한/영 헤더 지원. 제약률이 전부 1 미만이면 분수로 간주해 ×100. rate 셀이 전부 빈 행에서 테이블 종료. actual 없으면 실행갭 표시 자동 숨김.
- 상태는 localStorage(`opb-data-v1`)에 저장, Reset to sample로 초기화.
- **언어 전환(한/영)**: 헤더 우측 EN/한국어 토글. 선택은 localStorage `opb-lang`에 저장, 없으면 `navigator.language`로 자동 판별. 사전은 `I18N` 상수(en/ko), 헬퍼는 `T()`(단순) / `TF()`(치환) / `LX(obj,field)`(데이터 필드). HTML 정적 텍스트는 `data-i18n`(textContent) · `data-i18n-html`(innerHTML) · `data-i18n-ph`(placeholder) 속성으로 `applyStatic()`이 채움.
  - SAMPLE 데이터는 `nameKo`/`snameKo`/`shortKo`/`descKo`/`noteKo` 필드를 병기.
  - **사용자가 업로드/입력한 데이터는 번역하지 않고 원문 유지** (Ko 필드가 없으면 원본 필드로 폴백).
  - 주의: 차트 함수들이 지역변수 `L`을 좌표로 쓰므로 데이터 헬퍼 이름은 반드시 `LX` 유지.
- 샘플 데이터/제약은 index.html 안 `SAMPLE` 상수에 있음.

검증 기준값 — 동탄: 기준 212.0 → 최종 155.7, 실행 갭 +6.7 / 가중평균 195.4 → 142.1, 실적 143.8.

## 작업 규칙

- 한 파일(single-file HTML) 원칙 유지. 외부 CDN 스크립트 추가 금지.
- `index.html`을 고치면 **반드시 `404.html`에 복사** (`cp index.html 404.html`).
- 커밋 메시지는 한국어로 간결하게.
- 지표 정의(실행 갭 = 전체 제약 기준 고정)를 바꾸지 말 것 — 웹·엑셀이 같은 정의를 공유해야 함.
- 문서(`.md`)를 추가할 때는 `.assetsignore` 때문에 자동으로 비공개 처리됨 — 별도 조치 불필요.

## 모바일(claude.ai/code)에서 작업할 때

폰에서는 브라우저로 렌더링을 눈으로 확인하기 어렵다. 그래서:

1. **큰 구조 변경은 피하고** 문구 수정·수치 교체·색상 조정 같은 작은 변경 위주로.
2. push 전에 **문법 검증**을 반드시 거칠 것:
   - `node --check`는 HTML에 못 씀. 대신 `<script>` 블록을 추출해 검사하거나, 최소한 태그 짝·따옴표 짝을 확인.
   - i18n 키를 추가했으면 **en/ko 양쪽에 모두** 넣었는지 확인.
3. push하면 45초 뒤 실제 사이트가 바뀐다. 되돌리려면 `git revert HEAD && git push`.
4. 로컬 PC에만 있는 것(엑셀 모델, 구버전 한국어 대시보드)은 폰에서 볼 수 없음 — 그 내용이 필요하면 사용자에게 요청할 것.

## 남은 TODO

1. **Cloudflare Access** 적용 (사내 이메일 제한) — 실데이터 넣기 전 필수. 사용자가 Cloudflare 대시보드에서 직접 설정해야 함.
2. 실데이터 수령 시: `index.html`의 `SAMPLE` 상수 교체, 또는 사용자가 화면에서 엑셀 업로드.

---

**참고**: 사용자의 로컬 PC 프로젝트 루트에도 별도 `CLAUDE.md`가 있습니다(엑셀 모델 등 전체 산출물 관리용). 이 저장소 파일과 역할이 다르며, 대시보드 코드에 관한 내용을 고칠 때는 양쪽이 어긋나지 않게 주의하세요.
