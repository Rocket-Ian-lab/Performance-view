# 업무용 랩탑에서 대시보드 실행하기 (VS Code)

## 0. 준비 — 폴더를 VS Code로 열기

VS Code 실행 → 상단 메뉴 **File → Open Folder** → 이 `github-upload` 폴더 선택.

왼쪽에 `index.html`이 보이면 준비 끝입니다.

---

## 방법 1. Live Preview 확장 (가장 쉬움, 추천)

1. 폴더를 열면 오른쪽 아래에 **"이 폴더에 권장 확장이 있습니다"** 알림이 뜹니다 → **Install** 클릭.
   - 알림이 안 뜨면: 왼쪽 세로 아이콘 중 네모 4개 모양(확장) 클릭 → 검색창에 `Live Preview` 입력 → 만든 곳이 **Microsoft**인 것을 설치.
2. 왼쪽 파일 목록에서 `index.html` 을 **오른쪽 클릭** → **Show Preview** 클릭.
3. 브라우저가 열리고 주소가 `http://localhost:5500` 이면 성공입니다.

> 회사 정책으로 확장 설치가 막혀 있으면 방법 2로 가세요.

---

## 방법 2. 확장 없이 실행 (Python 이용)

1. VS Code 상단 메뉴 **Terminal → Run Task** 클릭.
2. 목록에서 **대시보드 실행 (Python)** 선택.
3. 아래 검은 창에 `Serving HTTP on ...` 이라고 뜨면 성공.
4. 브라우저 주소창에 직접 입력: **http://localhost:5500**

### 오류가 날 때

| 화면에 뜨는 말 | 해결 |
|---|---|
| `python is not recognized` / 명령을 찾을 수 없음 | Run Task에서 **대시보드 실행 (Python 대체 명령 py)** 를 대신 선택 |
| 둘 다 안 됨 (Python 자체가 없음) | **대시보드 실행 (Node)** 선택 |
| 셋 다 안 됨 | 방법 3으로 |

### 끄는 법
검은 창을 클릭하고 키보드에서 **Ctrl + C**. 또는 창 오른쪽의 휴지통 아이콘 클릭.

---

## 방법 3. 그냥 파일 더블클릭 (최후의 수단)

탐색기에서 `index.html` 더블클릭 → 브라우저로 열립니다.

**단, 이 방법은 제약이 있습니다:**
- 엑셀 업로드 기능이 브라우저 보안 정책에 막힐 수 있습니다.
- 입력한 데이터가 저장되지 않고 새로고침하면 사라질 수 있습니다.

보는 용도로만 쓰고, 데이터를 넣을 거면 방법 1이나 2를 쓰세요.

---

## 자주 묻는 것

**Q. 포트 5500이 이미 사용 중이라고 나옵니다.**
`.vscode/tasks.json` 파일을 열어 `5500`을 `5600` 같은 다른 숫자로 모두 바꾸세요. 주소도 `http://localhost:5600` 으로 바뀝니다.

**Q. 영문/국문은 어떻게 바꾸나요?**
화면 오른쪽 위 **EN / 한국어** 버튼을 누르세요. 처음 열 때는 브라우저 언어에 맞춰 자동으로 정해지고, 한 번 고르면 그 브라우저에 기억됩니다.
숫자와 계산 결과는 언어와 무관하게 완전히 동일합니다. 다만 **직접 업로드한 엑셀의 센터명·제약명은 번역되지 않고 입력한 그대로** 표시됩니다(원본을 임의로 바꾸지 않기 위해서입니다).

**Q. 글꼴이 평소와 좀 달라 보입니다.**
고장이 아닙니다. 사내망이 외부 글꼴 서버를 막으면 윈도우 기본 글꼴로 자동 대체됩니다. 화면 구성과 숫자는 전혀 영향받지 않습니다.

**Q. 인터넷이 끊겨도 되나요?**
됩니다. 이 대시보드는 서버와 통신하지 않습니다. 입력한 데이터도 밖으로 나가지 않고 브라우저 안에만 저장됩니다.

**Q. 내가 올린 엑셀 데이터는 어디에 저장되나요?**
그 브라우저 안에만 저장됩니다(localStorage). 다른 사람에게는 보이지 않습니다. 화면의 **Reset to sample** 버튼을 누르면 샘플 데이터로 되돌아갑니다.
