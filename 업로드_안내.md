# Performance-view 업로드 → Cloudflare 연결

저장소는 이미 만들어져 있습니다: https://github.com/Rocket-Ian-lab/Performance-view (현재 비어 있음)

이 폴더(`github-upload`)에 든 **5개 파일을 통째로 올리면** 끝입니다.

`index.html` · `404.html` · `_headers` · `robots.txt` · `README.md`

---

## 1단계 — GitHub에 파일 올리기 (1분)

1. https://github.com/Rocket-Ian-lab/Performance-view 접속
2. 비어 있는 화면 중간의 **uploading an existing file** 링크 클릭
   - 안 보이면: **Add file** → **Upload files**
3. 탐색기에서 이 폴더를 열고 **파일 5개를 전부 선택해 드래그**
   - 폴더째 끌지 말고 **안에 든 파일들**을 끌어야 저장소 루트에 올라갑니다
4. 아래 **Commit changes** 클릭

업로드가 끝나면 저장소 첫 화면에 README가 렌더링돼 보입니다.

---

## 2단계 — Cloudflare Pages 연결 (2분)

1. https://dash.cloudflare.com → **Compute (Workers & Pages)**
2. **Create** → **Pages** 탭 → **Connect to Git**
3. **Connect GitHub** → 계정 인증 → `Performance-view` 저장소 선택
4. 빌드 설정

   | 항목 | 값 |
   |---|---|
   | Project name | `performance-view` |
   | Production branch | `main` |
   | Framework preset | `None` |
   | Build command | **비워 둠** |
   | Build output directory | `/` |
   | Root directory | **비워 둠** |

5. **Save and Deploy** → 30초 내 완료
6. 최종 주소: `https://performance-view.pages.dev`

> Build command를 비우지 않으면 빌드 단계에서 실패합니다. 이 프로젝트는 정적 HTML 하나라 빌드가 필요 없습니다.

---

## 이후 갱신

| 하고 싶은 것 | 방법 |
|---|---|
| 숫자만 수정 | GitHub에서 `index.html` 열기 → 연필 아이콘 → `CENTERS` / `RATES` 수정 → Commit → 자동 재배포 |
| 새 버전 통째 교체 | Add file → Upload files → 새 `index.html` 드래그 → Commit |
| 되돌리기 | Cloudflare 프로젝트 → **Deployments** → 원하는 버전 → **Rollback** |

커밋 한 번마다 Cloudflare가 자동으로 새 버전을 배포하고, 실패해도 이전 버전이 계속 서비스됩니다.

---

## 접근 제한 (실제 실적 데이터 넣기 전에)

지금 설정은 검색엔진 노출만 차단(`robots.txt` + `X-Robots-Tag`)돼 있고, URL을 아는 사람은 열 수 있습니다.

1. Cloudflare → **Zero Trust** (최초 1회 팀 도메인 설정, Free 플랜 50명까지 무료)
2. **Access → Applications → Add an application → Self-hosted**
3. Application domain: `performance-view.pages.dev`
4. Policy: Action `Allow` → Include → **Emails ending in** → `@회사도메인.com`
